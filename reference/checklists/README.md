# Checklists

This directory holds short operational checklists derived from the subsystem guides.

These should be useful during implementation and review, for example:

- adding a new CLI command
- mounting a new router subtree
- writing API tests for a new endpoint
